{
  "name": "take-home-project-ai-powered-pdf-quiz-generatorreact",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:web\"",
    "dev:web": "cd apps/web && npm run dev",
    "dev:api": "cd apps/api && . venv/bin/activate && cd src && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "install:all": "npm run install:web && npm run install:api",
    "install:web": "cd apps/web && npm install",
    "install:api": "cd apps/api && python3 -m venv venv && . venv/bin/activate && pip install fastapi uvicorn python-multipart openai python-dotenv pydantic pymupdf",
    "build": "cd apps/web && npm run build",
    "start": "cd apps/web && npm run start",
    "clean": "rm -rf apps/web/node_modules apps/web/.next apps/api/venv"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "AI-Powered PDF Quiz Generator",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@chakra-ui/react": "^3.24.0",
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "framer-motion": "^12.23.12",
    "ls": "^0.2.1"
  }
}
